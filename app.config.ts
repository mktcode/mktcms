export default defineAppConfig({
  ui: {
    colors: {
      primary: 'sky',
      neutral: 'zinc'
    },
    navigationMenu: {
      slots: {
        linkLabel: 'hidden md:inline'
      }
    }
  }
})
